Drop the real images into THIS folder (public/assets) using these exact names:

  1. Profile photo.jpeg      -> Small profile photo in the HEADER (top-right). [already added]
  2. acharya-hero.jpeg       -> Large photo on the RIGHT of the HOME / hero section
                               (the namaste photo). Shown in a gold arched frame.
  3. headerbackground.jpg    -> Background image for the fixed header (optional).
  4. hero-bg.jpg             -> OPTIONAL real background photo for the hero. If present it
                               replaces the built-in temple/mandala artwork.

Notes
- Until acharya-hero.jpeg is added, a placeholder is shown automatically (nothing breaks).
- Files in public/ are served from the site root, referenced as "assets/<name>".
- Recommended sizes:
    acharya-hero.jpeg    ~ 800x1066 (portrait, 3:4)
    Profile photo.jpeg   ~ 400x400 (square)
    headerbackground.jpg ~ 1920x400 (wide)
    hero-bg.jpg          ~ 1600x800 (wide)
